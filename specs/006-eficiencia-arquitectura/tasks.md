# Tareas: 006 Eficiencia Global (Front y Worker)

- [ ] **Tarea 1**: Refactorizar `Cartones.tsx` en el frontend para quitar `framer-motion` de los items de la lista, reemplazando con CSS y `content-visibility: auto`.
- [ ] **Tarea 2**: Analizar `worker/pdf_processor.py` e instalar dependencias Node.js equivalentes en el backend (`pdf2pic`, `pdf-lib`, o similar).
- [ ] **Tarea 3**: Implementar la lógica real de recorte en `PdfProcessor.ts` de NestJS.
- [ ] **Tarea 4**: Eliminar por completo el worker de Python.
