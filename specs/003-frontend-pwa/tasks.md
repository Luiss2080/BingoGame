# Tareas: 003 Frontend UX/UI y PWA

- [x] **Tarea 1**: Instalar y configurar TailwindCSS en `frontend/` (Detectado V4 pre-instalado).
- [x] **Tarea 2**: Configurar el archivo `tailwind.config.js` con los *design tokens* (Ajustado en `index.css` para V4).
- [x] **Tarea 3**: Instalar `framer-motion` y refactorizar el componente principal de Cartones (o la vista principal) usando Tailwind y micro-animaciones.
- [x] **Tarea 4**: Implementar mutaciones optimistas con React Query para los endpoints de `vender` y `reservar`.
- [x] **Tarea 5**: Validar configuración de `vite-plugin-pwa` en `vite.config.ts`.
- [x] **Tarea 6**: Verificar levantando el frontend localmente y ejecutando tests si los hubiera.
